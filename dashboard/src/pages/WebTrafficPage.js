import React from "react";
import WebTraffic from "../components/WebTraffic";

export default function WebTrafficPage({ refreshTrigger }) {
    return <WebTraffic refreshTrigger={refreshTrigger} />;
}